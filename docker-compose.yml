services: {}

# This file is intentionally kept as a lightweight entry note.
# Use split compose files instead:
# - docker-compose.base.yml     (infra dependencies)
# - docker-compose.backend.yml  (java backend services)
# - docker-compose.frontend.yml (vue frontend service)

