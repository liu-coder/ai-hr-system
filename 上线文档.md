# AI HR 系统上线文档

**文档更新时间**：2026-04-02

## 1. 项目编译状态

✅ **编译成功**：所有7个模块均已成功编译，无编译错误。

## 2. 环境配置

### 2.1 系统要求
- JDK 17+
- Maven 3.6+
- MySQL 8.0+
- Redis 6.0+
- Milvus 2.6.14+
- 操作系统：Linux/Unix/Windows

### 2.2 环境变量配置

| 环境变量 | 说明 | 默认值 | 示例 |
|---------|------|--------|------|
| AIHR_MYSQL_HOST | MySQL主机地址 | localhost | 192.168.1.100 |
| AIHR_MYSQL_PORT | MySQL端口 | 3306 | 3306 |
| AIHR_MYSQL_USER | MySQL用户名 | root | admin |
| AIHR_MYSQL_PASSWORD | MySQL密码 | root | password |
| AIHR_REDIS_HOST | Redis主机地址 | localhost | 192.168.1.101 |
| AIHR_REDIS_PORT | Redis端口 | 6379 | 6379 |
| AIHR_MILVUS_HOST | Milvus主机地址 | localhost | 192.168.1.102 |
| AIHR_MILVUS_PORT | Milvus端口 | 19530 | 19530 |
| SPRING_AI_DASHSCOPE_API_KEY | DashScope API密钥 | - | sk-xxxxxxxxxxxx |
| AIHR_JWT_SECRET | JWT密钥 | - | your-secret-key |
| AIHR_EUREKA_HOST | Eureka服务地址 | localhost | 192.168.1.103 |
| AIHR_EUREKA_PORT | Eureka服务端口 | 8761 | 8761 |
| AIHR_CONFIG_HOST | 配置中心地址 | localhost | 192.168.1.104 |
| AIHR_CONFIG_PORT | 配置中心端口 | 8888 | 8888 |

### 2.3 数据库配置

#### 2.3.1 MySQL配置
1. 创建数据库：
   - ai_hr_attendance
   - ai_hr_salary
   - ai_hr_auth
   - ai_hr_ai_core

2. 数据库字符集：utf8mb4
3. 排序规则：utf8mb4_0900_ai_ci

#### 2.3.2 Redis配置
- 端口：6379
- 密码：（可选）
- 数据库：0

#### 2.3.3 Milvus配置
- 端口：19530
- 数据库：default
- 集合：ai_hr_policy_chunks

## 3. 上线脚本

### 3.0 本地快速启动（Docker 编排，含基础服务）

> 适用于本机一键拉起：基础设施 + 后端 + 前端 + 监控。

**步骤 1：启动基础服务（MySQL/Redis/Milvus 等）**

```powershell
docker compose -f E:/workspace/docker/docker-compose.base.yml up -d
```

**步骤 2：如需 RocketMQ（基础栈未包含时）**

```powershell
docker compose -f E:/workspace/docker/docker-compose.yml up -d
```

**步骤 3：启动后端服务**

```powershell
docker compose --env-file docker/base-images.env -f docker-compose.backend.yml up -d
```

**步骤 4：启动前端服务**

```powershell
docker compose -f docker-compose.frontend.yml up -d
```

**步骤 5：启动监控栈（Prometheus + Grafana）**

```powershell
docker compose -p irsai -f docker-compose.monitoring.yml up -d
```

**步骤 6：快速验证**

```powershell
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
```

常用访问地址：
- 前端：`http://localhost:5173`
- 网关：`http://localhost:9100`
- Prometheus：`http://localhost:9090`
- Grafana：`http://localhost:3000`（admin/admin）

### 3.1 编译脚本 `build.sh`

```bash
#!/bin/bash

# 设置环境变量
export JAVA_HOME=/path/to/java
export MAVEN_HOME=/path/to/maven
export PATH=$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH

# 编译项目
echo "开始编译项目..."
mvn clean package -DskipTests

if [ $? -eq 0 ]; then
    echo "编译成功！"
else
    echo "编译失败！"
    exit 1
fi

# 复制部署文件
echo "复制部署文件..."
mkdir -p deploy
cp -r ai-hr-*/target/*.jar deploy/
cp -r ai-hr-*/src/main/resources/application.yml deploy/

echo "编译脚本执行完成！"
```

### 3.2 启动脚本 `start.sh`

```bash
#!/bin/bash

# 设置环境变量
export AIHR_MYSQL_HOST=192.168.1.100
export AIHR_MYSQL_PORT=3306
export AIHR_MYSQL_USER=admin
export AIHR_MYSQL_PASSWORD=password
export AIHR_REDIS_HOST=192.168.1.101
export AIHR_REDIS_PORT=6379
export AIHR_MILVUS_HOST=192.168.1.102
export AIHR_MILVUS_PORT=19530
export AIHR_DASHSCOPE_API_KEY=sk-xxxxxxxxxxxx
export AIHR_JWT_SECRET=your-secret-key
export AIHR_EUREKA_HOST=192.168.1.103
export AIHR_EUREKA_PORT=8761
export AIHR_CONFIG_HOST=192.168.1.104
export AIHR_CONFIG_PORT=8888

# 启动顺序：1. 配置中心 2. 服务注册中心 3. 认证服务 4. 考勤服务 5. 薪酬服务 6. AI核心服务 7. 网关服务

echo "启动配置中心..."
nohup java -jar deploy/ai-hr-config-server-0.1.0-SNAPSHOT.jar > logs/config-server.log 2>&1 &
sleep 30

echo "启动服务注册中心..."
nohup java -jar deploy/ai-hr-eureka-server-0.1.0-SNAPSHOT.jar > logs/eureka-server.log 2>&1 &
sleep 30

echo "启动认证服务..."
nohup java -jar deploy/ai-hr-auth-0.1.0-SNAPSHOT.jar > logs/auth-server.log 2>&1 &
sleep 20

echo "启动考勤服务..."
nohup java -jar deploy/ai-hr-attendance-0.1.0-SNAPSHOT.jar > logs/attendance-server.log 2>&1 &
sleep 20

echo "启动薪酬服务..."
nohup java -jar deploy/ai-hr-salary-0.1.0-SNAPSHOT.jar > logs/salary-server.log 2>&1 &
sleep 20

echo "启动AI核心服务..."
nohup java -jar deploy/ai-hr-ai-core-0.1.0-SNAPSHOT.jar > logs/ai-core-server.log 2>&1 &
sleep 20

echo "启动网关服务..."
nohup java -jar deploy/ai-hr-gateway-0.1.0-SNAPSHOT.jar > logs/gateway-server.log 2>&1 &
sleep 20

echo "所有服务启动完成！"
```

### 3.3 停止脚本 `stop.sh`

```bash
#!/bin/bash

echo "停止所有服务..."

# 查找并停止所有Java进程
ps aux | grep java | grep ai-hr | awk '{print $2}' | xargs kill -9

echo "所有服务已停止！"
```

### 3.4 状态检查脚本 `status.sh`

```bash
#!/bin/bash

echo "检查服务状态..."

ps aux | grep java | grep ai-hr

echo "服务状态检查完成！"
```

## 4. 测试脚本

### 4.1 API测试脚本 `test-api.sh`

```bash
#!/bin/bash

# 获取Token
echo "获取认证Token..."
TOKEN=$(curl -s -X POST http://localhost:9010/auth/login -H "Content-Type: application/json" -d '{"username": "admin", "password": "admin123"}' | jq -r '.token')

if [ -z "$TOKEN" ]; then
    echo "获取Token失败！"
    exit 1
fi

echo "Token获取成功: $TOKEN"

# 测试考勤API
echo "\n测试考勤API..."
echo "1. 获取打卡记录:"
curl -X GET "http://localhost:9000/api/attendance/records?employeeId=emp-001&start=2024-03-01&end=2024-03-31" -H "Authorization: Bearer $TOKEN"

echo "\n2. 获取异常考勤:"
curl -X GET "http://localhost:9000/api/attendance/anomalies?employeeId=emp-001&start=2024-03-01&end=2024-03-31" -H "Authorization: Bearer $TOKEN"

# 测试薪酬API
echo "\n测试薪酬API..."
echo "1. 获取薪资政策:"
curl -X GET "http://localhost:9000/api/salary/policies" -H "Authorization: Bearer $TOKEN"

echo "\n2. 计算薪资:"
curl -X POST "http://localhost:9000/api/salary/calculate" -H "Content-Type: application/json" -H "Authorization: Bearer $TOKEN" -d '{"payPeriod": "2024-03", "employeeIds": ["emp-001"]}'

# 测试AI功能API
echo "\n测试AI功能API..."
echo "1. 智能排班优化:"
curl -X POST "http://localhost:9000/api/ai/attendance/optimize-scheduling" -H "Content-Type: application/json" -H "Authorization: Bearer $TOKEN" -d '{"startDate": "2024-04-01", "endDate": "2024-04-07", "employeeIds": ["emp-001", "emp-002"]}'

echo "\n2. 薪酬调整建议:"
curl -X POST "http://localhost:9000/api/ai/salary/suggest-adjustment" -H "Content-Type: application/json" -H "Authorization: Bearer $TOKEN" -d '{"employees": [{"employeeId": "emp-001", "currentSalary": 10000, "marketSalary": 12000, "performanceScore": 4.5, "tenureYears": 3}], "budgetLimit": 5000}'

echo "\nAPI测试完成！"
```

### 4.2 性能测试脚本 `performance-test.sh`

```bash
#!/bin/bash

# 使用ab工具进行性能测试
echo "开始性能测试..."

# 测试认证接口
echo "测试认证接口..."
ab -n 100 -c 10 -T application/json -p auth.json http://localhost:9010/auth/login

# 测试考勤接口
echo "\n测试考勤接口..."
TOKEN=$(curl -s -X POST http://localhost:9010/auth/login -H "Content-Type: application/json" -d '{"username": "admin", "password": "admin123"}' | jq -r '.token')
ab -n 100 -c 10 -H "Authorization: Bearer $TOKEN" "http://localhost:9000/api/attendance/records?employeeId=emp-001&start=2024-03-01&end=2024-03-31"

# 测试薪酬接口
echo "\n测试薪酬接口..."
ab -n 100 -c 10 -H "Authorization: Bearer $TOKEN" "http://localhost:9000/api/salary/policies"

# 测试AI接口
echo "\n测试AI接口..."
ab -n 10 -c 5 -T application/json -H "Authorization: Bearer $TOKEN" -p scheduling.json http://localhost:9000/api/ai/attendance/optimize-scheduling

echo "性能测试完成！"
```

## 5. 测试数据脚本

### 5.1 数据库初始化脚本 `init-db.sql`

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS ai_hr_attendance DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
CREATE DATABASE IF NOT EXISTS ai_hr_salary DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
CREATE DATABASE IF NOT EXISTS ai_hr_auth DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
CREATE DATABASE IF NOT EXISTS ai_hr_ai_core DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;

-- 授权
GRANT ALL PRIVILEGES ON ai_hr_*.* TO 'admin'@'%' IDENTIFIED BY 'password';
FLUSH PRIVILEGES;
```

### 5.2 测试数据脚本 `test-data.sql`

```sql
-- 考勤模块测试数据
USE ai_hr_attendance;

-- 插入考勤规则集
INSERT INTO attendance_rule_set (id, tenant_id, name, version, effective_from, effective_to, status, rule_json, created_at, created_by)
VALUES
('rule-set-001', 'tenant-001', '默认考勤规则', '1.0', '2024-01-01', NULL, 'ACTIVE', '{"rules": [{"name": "迟到规则", "type": "LATE_CHECKIN", "thresholdMinutes": 10}, {"name": "早退规则", "type": "EARLY_CHECKOUT", "thresholdMinutes": 10}, {"name": "旷工规则", "type": "ABSENT", "thresholdHours": 4}]}', CURRENT_TIMESTAMP, 'admin');

-- 插入打卡记录
INSERT INTO attendance_record (id, tenant_id, employee_id, work_date, check_in_at, check_out_at, source, raw_payload, created_at)
VALUES
('record-001', 'tenant-001', 'emp-001', '2024-03-25', '2024-03-25 09:00:00', '2024-03-25 18:00:00', 'APP', '{"deviceId": "device-001", "location": {"lat": 31.2304, "lng": 121.4737}}', CURRENT_TIMESTAMP),
('record-002', 'tenant-001', 'emp-001', '2024-03-26', '2024-03-26 09:30:00', '2024-03-26 17:30:00', 'APP', '{"deviceId": "device-001", "location": {"lat": 31.2304, "lng": 121.4737}}', CURRENT_TIMESTAMP),
('record-003', 'tenant-001', 'emp-002', '2024-03-25', '2024-03-25 08:50:00', '2024-03-25 18:10:00', 'APP', '{"deviceId": "device-002", "location": {"lat": 31.2304, "lng": 121.4737}}', CURRENT_TIMESTAMP);

-- 薪酬模块测试数据
USE ai_hr_salary;

-- 插入薪资政策
INSERT INTO salary_policy (id, tenant_id, name, version, effective_from, effective_to, status, policy_json, created_at, created_by)
VALUES
('policy-001', 'tenant-001', '默认薪资政策', '1.0', '2024-01-01', NULL, 'ACTIVE', '{"baseSalary": 10000, "allowances": [{"name": "交通补贴", "amount": 1000}, {"name": "餐补", "amount": 800}], "taxRate": 0.1}', CURRENT_TIMESTAMP, 'admin');

-- 插入薪资计算运行记录
INSERT INTO salary_input_snapshot (id, tenant_id, pay_period, snapshot_json, source_hash, created_at, created_by)
VALUES
('snapshot-001', 'tenant-001', '2024-03', '{"employees": [{"id": "emp-001", "baseSalary": 10000, "attendanceDays": 22, "overtimeHours": 10}, {"id": "emp-002", "baseSalary": 12000, "attendanceDays": 20, "overtimeHours": 5}]}', 'hash-001', CURRENT_TIMESTAMP, 'admin');

INSERT INTO salary_calc_run (id, tenant_id, pay_period, scope_type, scope_id, policy_id, calc_version, input_snapshot_id, result_hash, status, created_at, created_by)
VALUES
('calc-001', 'tenant-001', '2024-03', 'ALL', 'all', 'policy-001', '1.0', 'snapshot-001', 'result-hash-001', 'COMPLETED', CURRENT_TIMESTAMP, 'admin');

-- 插入薪资结果
INSERT INTO salary_result_line (id, tenant_id, calc_run_id, employee_id, item_code, item_name, amount_cents, currency, detail_json, created_at)
VALUES
('result-001', 'tenant-001', 'calc-001', 'emp-001', 'base_salary', '基本工资', 1000000, 'CNY', '{"rate": 1.0}', CURRENT_TIMESTAMP),
('result-002', 'tenant-001', 'calc-001', 'emp-001', 'traffic_allowance', '交通补贴', 100000, 'CNY', NULL, CURRENT_TIMESTAMP),
('result-003', 'tenant-001', 'calc-001', 'emp-001', 'meal_allowance', '餐补', 80000, 'CNY', NULL, CURRENT_TIMESTAMP),
('result-004', 'tenant-001', 'calc-001', 'emp-001', 'overtime_pay', '加班费', 50000, 'CNY', '{"hours": 10, "rate": 50}', CURRENT_TIMESTAMP),
('result-005', 'tenant-001', 'calc-001', 'emp-001', 'tax', '个人所得税', 123000, 'CNY', '{"rate": 0.1}', CURRENT_TIMESTAMP),
('result-006', 'tenant-001', 'calc-001', 'emp-002', 'base_salary', '基本工资', 1200000, 'CNY', '{"rate": 1.0}', CURRENT_TIMESTAMP),
('result-007', 'tenant-001', 'calc-001', 'emp-002', 'traffic_allowance', '交通补贴', 100000, 'CNY', NULL, CURRENT_TIMESTAMP),
('result-008', 'tenant-001', 'calc-001', 'emp-002', 'meal_allowance', '餐补', 80000, 'CNY', NULL, CURRENT_TIMESTAMP),
('result-009', 'tenant-001', 'calc-001', 'emp-002', 'overtime_pay', '加班费', 25000, 'CNY', '{"hours": 5, "rate": 50}', CURRENT_TIMESTAMP),
('result-010', 'tenant-001', 'calc-001', 'emp-002', 'tax', '个人所得税', 140500, 'CNY', '{"rate": 0.1}', CURRENT_TIMESTAMP);

-- 认证模块测试数据
USE ai_hr_auth;

-- 插入用户
INSERT INTO user (id, tenant_id, username, password, name, email, phone, status, created_at, created_by)
VALUES
('user-001', 'tenant-001', 'admin', '$2a$10$e1W4f5g6h7j8k9l0m1n2o3p4q5r6s7t8u9v0w1x2y3z4A5B6C7D8E9F0G', '管理员', 'admin@example.com', '13800138000', 'ACTIVE', CURRENT_TIMESTAMP, 'system');

-- 插入角色
INSERT INTO role (id, tenant_id, name, code, description, created_at, created_by)
VALUES
('role-001', 'tenant-001', '管理员', 'ADMIN', '系统管理员', CURRENT_TIMESTAMP, 'system'),
('role-002', 'tenant-001', 'HR专员', 'HR_STAFF', '人力资源专员', CURRENT_TIMESTAMP, 'system');

-- 插入用户角色关联
INSERT INTO user_role (user_id, role_id, created_at)
VALUES
('user-001', 'role-001', CURRENT_TIMESTAMP);
```

## 6. 部署步骤

### 6.1 准备阶段
1. 环境检查：确保所有依赖服务（MySQL、Redis、Milvus）已启动
2. 数据库初始化：执行 `init-db.sql` 创建数据库
3. 测试数据导入：执行 `test-data.sql` 导入测试数据

### 6.2 部署阶段
1. 编译项目：执行 `build.sh` 脚本编译项目
2. 配置环境变量：根据实际环境修改 `start.sh` 中的环境变量
3. 启动服务：执行 `start.sh` 脚本启动所有服务
4. 检查服务状态：执行 `status.sh` 脚本检查服务状态

### 6.3 测试阶段
1. API测试：执行 `test-api.sh` 脚本测试API功能
2. 性能测试：执行 `performance-test.sh` 脚本测试性能
3. 功能验证：验证所有核心功能是否正常

### 6.4 监控阶段
1. 日志监控：检查各服务日志
2. 指标监控：监控系统性能指标
3. 告警配置：配置必要的告警机制

## 7. 回滚方案

### 7.1 回滚步骤
1. 停止当前服务：执行 `stop.sh` 脚本
2. 恢复备份：恢复数据库和配置备份
3. 启动旧版本：启动之前的稳定版本

### 7.2 回滚触发条件
- 服务启动失败
- 核心功能异常
- 性能严重下降
- 数据丢失或损坏

## 8. 常见问题及解决方案

### 8.1 服务启动失败
- **原因**：端口被占用
- **解决方案**：检查端口使用情况，修改配置文件中的端口

### 8.2 数据库连接失败
- **原因**：数据库地址或凭证错误
- **解决方案**：检查环境变量配置，确保数据库服务正常运行

### 8.3 服务注册失败
- **原因**：Eureka服务未启动或网络问题
- **解决方案**：确保Eureka服务正常运行，检查网络连接

### 8.4 AI功能调用失败
- **原因**：DashScope API密钥错误或网络问题
- **解决方案**：检查API密钥配置，确保网络连接正常

### 8.5 性能问题
- **原因**：资源不足或配置不当
- **解决方案**：增加服务器资源，优化配置参数

## 9. 上线清单

### 9.1 环境准备
- [ ] 服务器资源准备
- [ ] 依赖服务部署
- [ ] 环境变量配置
- [ ] 网络配置

### 9.2 部署检查
- [ ] 编译成功
- [ ] 服务启动正常
- [ ] 数据库连接正常
- [ ] 服务注册成功

### 9.3 功能测试
- [ ] 认证功能
- [ ] 考勤功能
- [ ] 薪酬功能
- [ ] AI功能
- [ ] API接口

### 9.4 性能测试
- [ ] 响应时间
- [ ] 并发处理
- [ ] 资源占用
- [ ] 稳定性

### 9.5 监控配置
- [ ] 日志监控
- [ ] 指标监控
- [ ] 告警配置
- [ ] 备份策略

## 10. 总结

本上线文档提供了AI HR系统的完整上线方案，包括环境配置、部署脚本、测试脚本和回滚方案。通过按照文档中的步骤执行，可以确保系统顺利上线并稳定运行。同时，文档还提供了常见问题的解决方案，以应对上线过程中可能出现的问题。

在上线过程中，建议严格按照文档中的步骤执行，并在每个阶段进行充分的测试，以确保系统的稳定性和可靠性。
